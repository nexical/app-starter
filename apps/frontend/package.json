{
  "name": "@app/frontend",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "setup": "npm run setup:theme --workspace=app-core",
    "build": "npm run setup && astro build",
    "test": "npm run test --workspace=app-core",
    "dev": "npm run setup && astro dev",
    "preview": "astro preview"
  },
  "devDependencies": {
    "app-core": "*"
  },
  "dependencies": {
    "astro": "^5.17.1",
    "@nexical/sdk": "*"
  }
}